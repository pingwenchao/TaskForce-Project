<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Property defaults for log file naming and location -->
    <property name="LOG_FILE" value="${LOG_FILE:-${LOG_PATH:-${LOG_TEMP:-${java.io.tmpdir:-/tmp}}/}spring.log}"/>

    <!-- Console output configuration -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} - %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- File output configuration -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_FILE}</file>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_FILE}.%d{yyyy-MM-dd}.%i.gz</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <!-- Application-specific loggers -->
    <!-- TaskForce application logger (INFO level for detailed audit) -->
    <logger name="com.upm.taskforce" level="INFO"/>

    <!-- Security and authentication logger (DEBUG for authentication flow) -->
    <logger name="org.springframework.security" level="INFO"/>

    <!-- JPA/Hibernate logger (WARN to reduce verbosity) -->
    <logger name="org.hibernate" level="WARN"/>

    <!-- Spring Framework logger (INFO for application insights) -->
    <logger name="org.springframework" level="INFO"/>

    <!-- Root logger configuration -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>

    <!-- Development profile: enable more verbose logging -->
    <springProfile name="dev">
        <logger name="com.upm.taskforce" level="DEBUG"/>
        <logger name="org.springframework.security" level="DEBUG"/>
    </springProfile>

    <!-- Production profile: reduce console output, keep file logging -->
    <springProfile name="prod">
        <root level="WARN">
            <appender-ref ref="FILE"/>
        </root>
    </springProfile>
</configuration>

